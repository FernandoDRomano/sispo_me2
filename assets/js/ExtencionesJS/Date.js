/*
$(function () {
	$('.FechaHoraMinuto').datetimepicker({
		format: 'DD/MM/YYYY HH:mm',locale: 'en',
		date: new Date()
	});
	
});

$(function () {
	$('.Fecha').datetimepicker({
		format: 'YYYY/MM/DD',locale: 'en',
		date: new Date()
	});
	
});

$(function () {
	$('.FechaHoraMinutoOpen').datetimepicker({
		format: 'DD/MM/YYYY HH:mm:00',locale: 'en',//,
		inline: true,
		sideBySide: true
	})
});

$(function () {
	$('.FechaFull').datetimepicker({
		format: 'DD/MM/YYYY HH:mm:ss',locale: 'en'
	})
});

$(function () {
	$('.FechaFullRegular').datetimepicker({
		format: 'YYYY/MM/DD HH:mm:ss',locale: 'en'
	})
});
*/
